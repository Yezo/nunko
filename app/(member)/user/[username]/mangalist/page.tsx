import { Main } from "@/components/layout/main"
import { UserProfileNavigation } from "@/components/navbar/tabs/tab"

export default async function UserMangaListPage({ params }: { params: { username: string } }) {
  return <>Manga list</>
}
